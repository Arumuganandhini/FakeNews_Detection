// Registers the jest-dom matchers (toBeInTheDocument, toBeDisabled, and the
// rest) for every test file. Create React App loads this automatically before
// the suite runs; without it those matchers are simply absent.
import '@testing-library/jest-dom';
